# US KIS live/mock report schedule — 운영 종료

KIS cutover로 `kis_live` advisory bundle과 `kis_mock` preview bridge, credential 주입 및 schedule 실행 절차는 폐기되었다. 이미 생성된 report/snapshot/ledger 행은 원래 account scope와 provenance로 보존하며 Toss로 자동 재해석하지 않는다.

새 US equity 계좌·시장 evidence는 Toss 경계를 사용한다. NH PLUG는 KR mock read-only이므로 US report 또는 주문 기능의 대체 provider로 사용하지 않는다.
