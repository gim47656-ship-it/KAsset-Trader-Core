"""Fork-local runtime extensions.

Keep KAsset-specific code additive so upstream auto_trader updates remain easy to
merge. Runtime extensions must not bypass upstream safety or broker boundaries.
"""
